apiVersion: apps/v1
kind: Deployment
metadata:
  name: lcp-server
spec:
  template:
    spec:
      containers:
        - name: lcp-server
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 768Mi
