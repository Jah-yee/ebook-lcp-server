apiVersion: v1
kind: ConfigMap
metadata:
  name: lcp-config
data:
  LCP_PROFILE: production
  PUBLIC_BASE_URL: https://testmedical.ir
  LCP_PROVIDER_URI: https://testmedical.ir
  STATUS_BASE_URL: https://status.testmedical.ir
